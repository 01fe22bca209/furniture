# Copy this file to .env if you need to point the frontend to a different API.

# Only set this when the API is NOT on the same host (e.g. backend on another machine or port).
# Examples:
#   Same machine, backend on 5000: leave empty (uses package.json proxy in dev)
#   Backend elsewhere: http://localhost:5000  or  https://your-api.com
# Do not add /api at the end; the app adds it.
REACT_APP_API_URL=
